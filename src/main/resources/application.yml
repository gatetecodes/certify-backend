spring:
  application:
    name: certify-backend

  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/certify}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:Magnolia!}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
    open-in-view: false

  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info

certify:
  storage:
    base-path: storage
  security:
    jwt:
      secret: ${JWT_SECRET:}
      expiration-minutes: 60
  verification:
    base-url: ${CERTIFY_VERIFICATION_BASE_URL:http://localhost:8080/public/verify}
